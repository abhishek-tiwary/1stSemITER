public class CMD_Q1_Division {
	public static void main(String[] args) {
		int a=Integer.parseInt(args[0]);
		int b=Integer.parseInt(args[1]);
		System.out.println("a= "+ a+ "\tb= " +b);
		float c=(float) a/b;
		System.out.println("c= "+c);
		
		/* Right click on program space
		 * Run As --> Run Congurations
		 * Check class,project, main class, name properly
		 * Click on arguments
		 * Pass reqd no. of values in program arguments (separate by spaces) & click run
		*/ 
	}

}